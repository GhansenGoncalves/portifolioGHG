# Testes de API

Coleções Postman e exemplos de testes de API (contrato, status code,
schema e cenários negativos).

## Estrutura sugerida

- `collections/` — coleções Postman exportadas (.json)
- `environments/` — variáveis de ambiente por estágio
